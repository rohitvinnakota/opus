<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.opus.www.opus">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateNewSong"
            android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".InstrumentSelect" android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".SongListActivity" android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".TrackViewActivity" android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".VocalActivity" android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".PianoActivity" android:theme="@style/AppFullScreenTheme"/>
        <activity android:name=".DrumsActivity" android:theme="@style/AppFullScreenTheme"/>
    </application>


</manifest>